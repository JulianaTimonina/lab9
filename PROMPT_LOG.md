# Prompt Log — Лабораторная работа №9, Вариант 9

## Задание М1: Добавить горутину для фоновой обработки запросов

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши HTTP-сервер на Go, который: Принимает POST запросы на эндпоинт /process. 
Ожидает JSON в формате {"numbers": [1, 2, 3, 4, 5]}. В ответе должен быть JSON с 
полями: sum_squares: 0 (возвращается сразу), message: "Запрос принят, обработка 
в фоне". Запускает горутину, которая: Имитирует тяжелую обработку с помощью 
time.Sleep(2 секунды). Вычисляет сумму квадратов переданных чисел. Выводит результат 
в лог через log.Printf. Напиши Python-клиент: отправляет POST запрос к этому серверу 
и выводит ответ. Создай go.mod для управления зависимостями.
```

**Результат:** Был получен работающий HTTP-сервер на Go с горутиной

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Нужно добавить юнит-тесты для проверки всех функций
```

**Результат:** Получены тесты для проверки решения. Работает, тесты проходит

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: ничего
- Время: ~30 мин

---

## Задание М2: Передать данные из Python в Go через JSON (stdin/stdout)

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Go-программа calculator: Читает JSON из stdin, вычисляет сумму чисел и сумму 
квадратов, возвращает результат в JSON через stdout, обработка ошибки некорректного 
ввода.
Python-скрипт: Запускает Go-бинар как подпроцесс, передаёт данные через stdin, 
получает результат из stdout, должен быть класс-обёртка для взаимодействия.
Юнит-тесты: Проверяют работу Go бинаря через subprocess; тестируют разные сценарии: 
обычные числа, отрицательные, пустой список, одно число, большие числа, нули; 
проверяют обработку ошибок.
```

**Результат:** Работает, тесты проходит

### Итого

- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 мин

---

## Задание М3: Сравнить скорость выполнения Rust-функции и аналогичной на Python

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Напиши программу для сравнения производительности Rust и Python.
Создай Rust библиотеку с функциями: сумма квадратов чисел, произведение чисел, 
Фибоначчи (рекурсивный), Фибоначчи (итеративный), сортировка списка, фильтрация 
чётных чисел; используй PyO3 для создания Python-модуля из этой библиотеки; напиши 
Python скрипт, который сравнивает скорость выполнения этих функций на Rust и Python, 
выводит время и ускорение; напиши юнит-тесты для проверки корректности Rust функций
Технические требования:
Rust: библиотека собирается как cdylib, PyO3 версии 0.23 (поддерживает Python 3.13)
Python: скрипт запускает функции, замеряет время через time.perf_counter
Тесты: unittest для проверки результатов Rust функцй
```

**Результат:** 
- Не собиралось из-за конфликта версий, исправлено. 
- После исправления работает, тесты проходит
- Rust работает быстрее (среднее ускорение в 9.16 раз)

### Итого

- Количество промптов: 1
- Что пришлось исправлять вручную: исправлена версия в Cargo.toml
- Время: ~30 мин

---

## Задание В1: Настроить сборку Rust-модуля в CI/CD и публикацию на PyPI

### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Создай Rust библиотеку с Python биндингами через PyO3 для математических операций. 
Название библиотеки: lab9-rust-math-lib.
Требования к функциям: sum_squares(numbers: Vec<i64>) -> i64 - возвращает сумму квадратов
 чисел, factorial(n: u64) -> u64 - вычисляет факториал числа, 
 quick_sort(mut numbers: Vec<i64>) -> Vec<i64> - сортирует массив чисел, 
 fibonacci(n: u32) -> u64 - возвращает n-е число Фибоначчи, MathOperations - класс с 
 полем value: f64 и методами square() и cube()
Создай полную структуру проекта:
- Cargo.toml с правильными зависимостями (pyo3 версии 0.23.5)
- pyproject.toml для сборки через maturin
- src/lib.rs с реализацией всех функций
- tests/test_rust_math.py с 5 тестами
```

**Результат:**  Rust модуль работает

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Настрой CI/CD через GitHub Actions для автоматической публикации Rust библиотеки на 
PyPI. Библиотека находится в папке h1/rust_math_lib/.
Требования:
1. Создай файл .github/workflows/ci-cd.yml
2. Workflow должен запускаться при создании тегов формата v* (v1.0.0, v0.1.0 и т.д.), должен запускаться только при изменениях в папке h1/rust_math_lib/**
3. Публикация на PyPI через maturin publish
4. Используй секрет PYPI_API_TOKEN для аутентификации
```

**Результат:**  
- Получен файл ci-cd.yml
- Найдена ошибка в ci-cd.yml, исправлена
- На GitHub после исправления все работает, тесты проходит
- Успешно опубликованно на PyPI

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: исправлена ошибка в ci-cd.yml
- Время: ~40 мин

---

## Задание В2: Профилировать и сравнить производительность трёх решений: Python, Python+Rust, Python+Go (как внешний сервис)


### Промпт 1

**Инструмент:** DeepSeek

**Промпт:**
```
Необходимо выполнить профилирование и сравнение производительности трёх решений:
1. Чистая реализация на Python
2. Python с расширением на Rust (с использованием PyO3)
3. Python с внешним сервисом на Go
Реализуй следующие компоненты:
- Модуль на Python с функциями: сумма квадратов, полная обработка данных (сумма, 
сумма квадратов, максимум, минимум, среднее), итеративное вычисление чисел Фибоначчи
- Библиотеку на Rust с биндингами PyO3, реализующую те же функции, скомпилированную 
как cdylib
- HTTP сервис на Go с эндпоинтами /process, /heavy и /health, реализующий те же 
вычисления
- Скрипт для бенчмарка, который тестирует все три решения с разными размерами данных 
и множественными итерациями, измеряя время выполнения со статистикой
Все реализации должны использовать одинаковый итеративный алгоритм Фибоначчи для 
честного сравнения.
```

**Результат:**  
- Исправлен конфликт версий при сборке
- Выполняется сравнение производительности решений

### Промпт 2

**Инструмент:** DeepSeek

**Промпт:**
```
Добавь юнит-тесты для всех трёх реализаций.
Создай:
- Юнит-тесты для Python модуля, покрывающие все функции (сумма квадратов, обработка 
чисел, Фибоначчи, тяжёлые вычисления), включая граничные случаи (пустой список, один 
элемент, отрицательные числа)
- Юнит-тесты для Rust модуля, тестирующие ту же функциональность, с корректной 
обработкой формата возвращаемых данных
- Юнит-тесты для Go сервиса, которые тестируют все эндпоинты, включая обработку 
ошибок (неверный JSON, неправильный HTTP метод)
- Скрипт для запуска всех тестов с выводом сводки результатов
Включи проверку граничных случаев, таких как пустые списки.
```

**Результат:**  Работает, тесты проходит

### Итого

- Количество промптов: 2
- Что пришлось исправлять вручную: исправлен конфликт версий в Cargo.toml
- Время: ~35 мин

---

## Вывод

**Для будущих проектов:**
- Проверять совместимость версий (особенно PyO3 с Python 3.13)
- Подробнее разобраться со сборкой и запуском модулей Rust

**Вывод:** AI-инструменты эффективны для генерации архитектуры кода и значительно 
экономят время и силы. Однако реальное исполнение такого кода сопряжено с нюансами 
окружения: конкретными версиями интерпретаторов и компиляторов, политиками безопасности 
операционной системы, переменными окружения, путями доступа к зависимостям и библиотекам. 
Эти аспекты требуют от разработчика ручной отладки, понимания системных вызовов и 
готовности адаптировать сгенерированный код под реальные условия.